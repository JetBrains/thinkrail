import { CollapsibleSection } from "../CollapsibleSection.tsx";

export function LinkedTasks() {
  return (
    <CollapsibleSection title="Tasks">
      <div className="section-placeholder">Tasks linked to this spec will appear here</div>
    </CollapsibleSection>
  );
}
