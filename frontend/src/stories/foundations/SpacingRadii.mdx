import { Meta } from "@storybook/addon-docs/blocks";
import { SpacingScale, CornerRadii, Transitions } from "./_spacing-radii";

<Meta title="Foundations/Spacing & Radii" />

# Spacing & Radii

Spacing, corner radii and transition tokens from `src/styles/tokens.css`. Like
the type scale, spacing and radii are `calc()` ratios of `--font-base`, so they
scale together with the base font size.

## Spacing scale

Ratios of `--font-base`. Bar width = the token value.

<SpacingScale />

## Corner radii

<CornerRadii />

## Transitions

Hover a card to feel the timing.

<Transitions />
