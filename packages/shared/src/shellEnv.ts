// Resolve the user's PATH for a GUI-launched process so the in-process agent's bash/tools
// find git/node/etc. Implemented when M10 (engine host) lands; M0 stub.

export function resolveShellEnv(): void {}
