{
	"name": "pi-delegation",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"exports": {
		".": "./index.ts",
		"./index.ts": "./index.ts",
		"./package.json": "./package.json"
	},
	"description": "The session fabric for pi: create agent sessions from agent sessions — one creation primitive, a run-owning handle, lineage, a run registry, and lifecycle events.",
	"keywords": [
		"pi-package",
		"pi",
		"pi-coding-agent",
		"delegation",
		"subagents"
	],
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "bun test"
	},
	"peerDependencies": {
		"@earendil-works/pi-agent-core": "*",
		"@earendil-works/pi-ai": "*",
		"@earendil-works/pi-coding-agent": "*"
	},
	"devDependencies": {
		"@earendil-works/pi-agent-core": "catalog:",
		"@earendil-works/pi-ai": "catalog:",
		"@earendil-works/pi-coding-agent": "catalog:",
		"@types/bun": "catalog:",
		"typebox": "catalog:",
		"typescript": "catalog:"
	}
}
