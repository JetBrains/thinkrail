name: Build ThinkRail native artifacts
description: >-
  Stamps the shared release identity, builds the web UI, compiles and smokes the native CLI binary,
  packages and smokes the native Electrobun desktop app, and exposes both release artifacts.

inputs:
  version:
    description: "Version to bake into both launchers (e.g. 0.2.0-nightly.5)"
    required: true
  channel:
    description: "Release channel: nightly or stable"
    required: true
  target:
    description: "Bun compile target matching this native runner"
    required: true
outputs:
  artifact-name:
    description: "CLI artifact filename"
    value: ${{ steps.resolve-cli.outputs.name }}
  artifact-path:
    description: "CLI artifact path"
    value: ${{ steps.resolve-cli.outputs.path }}
  desktop-artifact-name:
    description: "Desktop first-install artifact filename"
    value: ${{ steps.resolve-desktop.outputs.name }}
  desktop-artifact-path:
    description: "Desktop first-install artifact path"
    value: ${{ steps.resolve-desktop.outputs.path }}
  desktop-app-archive-path:
    description: "Official expanded macOS app archive for private signing; empty on other targets"
    value: ${{ steps.resolve-desktop.outputs.app-archive-path }}
  desktop-update-manifest-path:
    description: "Official Electrobun update manifest with its framework-generated filename"
    value: ${{ steps.resolve-desktop.outputs.update-manifest-path }}
  desktop-update-archive-path:
    description: "Official Electrobun full update archive with its framework-generated filename"
    value: ${{ steps.resolve-desktop.outputs.update-archive-path }}

runs:
  using: composite
  steps:
    - name: Stamp shared release identity
      shell: bash
      env:
        VERSION: ${{ inputs.version }}
        CHANNEL: ${{ inputs.channel }}
      run: |
        set -eu
        commit=$(git rev-parse --short HEAD)
        cat > packages/shared/src/version.ts <<EOF
        export const version = "${VERSION}";
        export const channel = "${CHANNEL}";
        export const commit = "${commit}";
        EOF
        cat packages/shared/src/version.ts

    - name: Build web UI
      shell: bash
      run: bun run build:web

    - name: Compile single-file CLI binary
      shell: bash
      env:
        TARGET: ${{ inputs.target }}
      run: bun apps/cli/scripts/build-binary.ts --target="$TARGET"

    - id: resolve-cli
      name: Resolve CLI artifact
      shell: bash
      env:
        TARGET: ${{ inputs.target }}
      run: |
        set -eu
        name="thinkrail-${TARGET#bun-}"
        case "$TARGET" in *windows*) name="${name}.exe" ;; esac
        path="apps/cli/dist/${name}"
        [ -f "$path" ] || { echo "::error::expected CLI artifact not found at $path"; exit 1; }
        {
          echo "name=$name"
          echo "path=$path"
        } >> "$GITHUB_OUTPUT"

    - name: Smoke CLI binary
      shell: bash
      run: bun run smoke:binary "${{ steps.resolve-cli.outputs.path }}"

    - name: Build and smoke desktop app
      shell: bash
      env:
        TARGET: ${{ inputs.target }}
      run: |
        set -eu
        bun run desktop:build
        launcher=$(bun -e 'import { locateDesktopLauncher } from "@thinkrail/artifact-tests"; console.log(locateDesktopLauncher("apps/desktop"));')
        case "$TARGET" in
          bun-linux-*)
            env LIBGL_ALWAYS_SOFTWARE=1 WEBKIT_DISABLE_COMPOSITING_MODE=1 \
              xvfb-run -a bun run smoke:desktop "$launcher"
            ;;
          *) bun run smoke:desktop "$launcher" ;;
        esac

    - name: Package desktop installer
      shell: bash
      env:
        TARGET: ${{ inputs.target }}
        CHANNEL: ${{ inputs.channel }}
      run: |
        set -eu
        if [ "$TARGET" = bun-windows-x64 ]; then
          system32="$(cygpath -u "$SYSTEMROOT")/System32"
          export PATH="$system32:$PATH"
          echo "Electrobun tar: $(command -v tar)"
        fi
        case "$CHANNEL" in
          nightly) environment="canary" ;;
          stable) environment="stable" ;;
          *) echo "::error::unsupported release channel $CHANNEL"; exit 1 ;;
        esac
        bun run --cwd apps/desktop "package:$environment"

    - id: resolve-desktop
      name: Resolve desktop installer
      shell: bash
      env:
        TARGET: ${{ inputs.target }}
        CHANNEL: ${{ inputs.channel }}
      run: |
        set -eu
        mkdir -p apps/desktop/dist
        case "$CHANNEL" in
          nightly) environment='canary'; prefix='canary-'; suffix='-canary' ;;
          stable) environment='stable'; prefix=''; suffix='' ;;
          *) echo "::error::unsupported release channel $CHANNEL"; exit 1 ;;
        esac
        app_archive_path=''
        case "$TARGET" in
          bun-darwin-arm64)
            platform='macos-arm64'
            source="${prefix}${platform}-ThinkRail${suffix}.dmg"
            name='thinkrail-desktop-darwin-arm64.dmg'
            update_archive_name="${environment}-${platform}-ThinkRail${suffix}.app.tar.zst"
            app_archive_path="apps/desktop/artifacts/$update_archive_name"
            ;;
          bun-windows-x64)
            platform='win-x64'
            source="${prefix}${platform}-ThinkRail-Setup${suffix}.zip"
            name='thinkrail-desktop-windows-x64.zip'
            update_archive_name="${environment}-${platform}-ThinkRail${suffix}.tar.zst"
            ;;
          bun-linux-x64)
            platform='linux-x64'
            source="${prefix}${platform}-ThinkRail${suffix}-Setup.tar.gz"
            name='thinkrail-desktop-linux-x64.tar.gz'
            update_archive_name="${environment}-${platform}-ThinkRail${suffix}.tar.zst"
            ;;
          bun-linux-arm64)
            platform='linux-arm64'
            source="${prefix}${platform}-ThinkRail${suffix}-Setup.tar.gz"
            name='thinkrail-desktop-linux-arm64.tar.gz'
            update_archive_name="${environment}-${platform}-ThinkRail${suffix}.tar.zst"
            ;;
          *) echo "::error::unsupported desktop target $TARGET"; exit 1 ;;
        esac
        if [ -n "$app_archive_path" ]; then
          [ -f "$app_archive_path" ] || { echo "::error::desktop app archive not found at $app_archive_path"; exit 1; }
        fi
        source="apps/desktop/artifacts/$source"
        [ -f "$source" ] || { echo "::error::desktop installer not found at $source"; exit 1; }
        update_manifest_path="apps/desktop/artifacts/${environment}-${platform}-update.json"
        [ -f "$update_manifest_path" ] || { echo "::error::desktop update manifest not found at $update_manifest_path"; exit 1; }
        update_archive_path="apps/desktop/artifacts/$update_archive_name"
        [ -f "$update_archive_path" ] || { echo "::error::desktop update archive not found at $update_archive_path"; exit 1; }
        path="apps/desktop/dist/$name"
        cp "$source" "$path"
        {
          echo "name=$name"
          echo "path=$path"
          echo "app-archive-path=$app_archive_path"
          echo "update-manifest-path=$update_manifest_path"
          echo "update-archive-path=$update_archive_path"
        } >> "$GITHUB_OUTPUT"

    - name: Smoke desktop first-install artifact
      shell: bash
      env:
        CHANNEL: ${{ inputs.channel }}
        TARGET: ${{ inputs.target }}
      run: |
        set -eu
        case "$CHANNEL" in
          nightly) environment="canary" ;;
          stable) environment="stable" ;;
          *) echo "::error::unsupported release channel $CHANNEL"; exit 1 ;;
        esac
        case "$TARGET" in
          bun-linux-*)
            env LIBGL_ALWAYS_SOFTWARE=1 WEBKIT_DISABLE_COMPOSITING_MODE=1 \
              xvfb-run -a bun run smoke:desktop:installer \
              "${{ steps.resolve-desktop.outputs.path }}" "$environment"
            ;;
          *)
            bun run smoke:desktop:installer \
              "${{ steps.resolve-desktop.outputs.path }}" "$environment"
            ;;
        esac
