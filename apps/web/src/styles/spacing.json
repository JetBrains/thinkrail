{
	"$schema": "./spacing.schema.json",
	"metadata": {
		"version": "1.0.0",
		"note": "Canonical numeric spacing scale. A step name IS its pixel value, so an instruction ('use spacing 8') maps to exactly one token (--space-8) and one utility family (p-8, gap-8). 0 is the identity step (no spacing). Do not add a step (e.g. 6) without an explicit design decision."
	},
	"steps": {
		"0": "0px",
		"2": "2px",
		"4": "4px",
		"8": "8px",
		"12": "12px",
		"16": "16px",
		"24": "24px"
	}
}
