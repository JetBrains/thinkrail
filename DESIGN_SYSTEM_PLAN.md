# Design System Extraction — Plan

_Generated by `/design-system-extract` Phase 0 (Discovery). No code changed yet._

## Codebase snapshot
- **Roots scanned:** `frontend/src/` (components in `frontend/src/components/`, tokens in `frontend/src/styles/`)
- **Styling approach:** Plain **CSS files**, one per component (~37 `*.css`), all consuming CSS custom properties. No CSS Modules, no styled-components, no Tailwind. 41 files use inline `style={{}}`.
- **Framework:** React 19 + Vite 6 + TypeScript. State via Zustand. Icons via `lucide-react`.
- **Component count:** 120 `.tsx` files across 21 feature folders.
- **Existing tokens:** **Yes — central & mature.** `styles/tokens.css` (font/spacing/radius scales derived from `--font-base` via `calc()`, brand accent colors, transitions) + a semantic color layer in `theme-dark.css` / `theme-light.css` / `themes.css` covering **7 themes** (dark, light, high-contrast, dracula, nord, solarized ×2, claude-code).
- **Storybook:** **Absent** — no `.storybook/`, no `*.stories.*`, nothing in `package.json` (despite the `arina/storybook` branch name).

## Maturity assessment
| Dimension | Score | Evidence |
|-----------|-------|----------|
| **Tokens** | **4/4** | 3,379 `var(--token)` uses, 52 distinct tokens, central source w/ derived scales + 7-theme semantic layer. Minor drift: ~80 distinct hardcoded hex remain (`#fff` ×21, `#3b74ee` ×10 duplicates `--blue`, accent dupes). |
| **Components** | **1/4** | **No shared primitives.** 221 raw `<button>` with dozens of ad-hoc per-feature classes (`.np-form-btn`, `.token-dialog-btn-primary`, `.welcome-start-btn`…). 20 raw `<input>`, no `Input`. Only `Modal`, `Dropdown`, `CopyButton` shared — and barely reused (≤4×). |
| **Consistency** | **2/4** | Colors mostly tokenized, but button styling is copy-pasted everywhere with hardcoded `padding`/`radius`/`font-size`/colors (e.g. `.np-form-btn`: `padding:10px 24px; border-radius:8px; background:#4d8d4a`). Spacing often raw px (8/6/4/12/16) instead of `--space-*`. |
| **Documentation** | **0/4** | No Storybook, no living docs. Tokens documented only via CSS comments. |

**Overall:** A **token-mature, component-immature** codebase. The visual foundation (colors, type, spacing, theming) is genuinely strong and centralized — that's done well. The pain is one layer up: there are **no shared UI primitives**, so the same button is hand-rolled 200+ times with drifting padding/colors. That makes documentation (Storybook) high-value and a **Button extraction (Step 5) the single biggest win**. Token refactoring (Step 4) is minor cleanup, not foundational work.

## Environment
- **App running:** Something is listening on `:3000` and `:8000` (via Docker) — _need to confirm with user_ this is ThinkRail.
- **Storybook running:** No (not installed).
- **Playwright MCP:** Available this session ✓ (will be used for before/after verification on refactor steps).

## Proposed steps (tailored to THIS codebase)
- [ ] **Step 1 — Storybook:** Install `storybook@latest` for the **react-vite** framework. Wire it to load `styles/global.css` so stories inherit tokens + themes; add a theme switcher (`data-theme`) toolbar so stories can be previewed across the 7 themes.
- [ ] **Step 2 — Existing tokens (additive):** Build "Foundations" stories that visualize the *real* tokens from `tokens.css`/`themes.css` — color swatches (accents + per-theme semantics), type scale, spacing scale, radii, transitions. Annotate with usage counts.
- [ ] **Step 3 — Existing components (additive):** Add stories for the 3 real shared components — `Modal`, `Dropdown`, `CopyButton` — plus the most reusable `shared/` selectors (`FileSelector`, `SpecSelector`, `TicketSelector`, `SkillGrid`). Stories only, no component changes.
- [ ] **Step 4 — New tokens (refactor, suggest→confirm):** Minor cleanup only — dedupe hardcoded hex that already have tokens (`#3b74ee`→`--blue`, accent dupes, `#fff`), and propose mapping the most common raw spacing px (8/6/4/12px) to `--space-*`. **Low priority** given Tokens = 4/4; offer to skip.
- [ ] **Step 5 — New components (refactor, suggest→confirm):** **The main event.** Extract a shared **`Button`** primitive (variants: primary / secondary / danger / ghost / icon; sizes; loading/disabled) covering the patterns found across the 221 raw buttons, token-based. Then migrate a first batch of call sites. Likely also a **`Input`** primitive. Each migration verified visually identical via Playwright.

## Risks & notes
- Steps 1–3 are **additive** (config + stories + docs only) — they will not touch existing components or styles.
- Steps 4–5 **modify existing code** — each change is suggested with a diff/plan first, confirmed, then applied, then verified before/after with Playwright so the app stays visually identical.
- Commit after each step (scoped message) to keep every step reversible.
- Given the maturity profile, it's reasonable to stop after Step 3 (full documentation) if the refactors aren't wanted now.
